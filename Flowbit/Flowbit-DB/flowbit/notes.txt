System Registration JSON via system:
{
	"SystemName":(str),
	"Location": {"longitude": (float), "latitude": (float)}, #tricky
	"MACAddress": (str),
	"IPAddress": (str),
	"DateRequested": (int)
}

System Registration JSON via App:
{
	"Sensors": [
		("SensorName", (int)(pin number))
	]
}